{
  "type": "object",
  "properties": {
    "inpath": {
      "type": "string",
      "title": "Input data",
      "default": "",
      "description": "The full path to the file containing complex information"
    },
    "maskpath": {
      "type": "string",
      "title": "Mask file",
      "default": "",
      "description": "The full path to the file with the mask"
    },
    "normalise": {
      "type": "boolean",
      "title": "Normalise data",
      "default": false,
      "description": "Select to apply normalisation"
    },
    "outpath": {
      "type": "string",
      "title": "Output folder",
      "default": "",
      "description": "The full path to the output folder"
    }
  },
  "required": ["inpath"]
}
